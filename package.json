{
  "name": "express-template",
  "displayName": "Express Template",
  "description": "Uma extensão para criar estrutura de projeto Express.js",
  "version": "0.0.1",
  "engines": {
      "vscode": "^1.56.0"
  },
  "activationEvents": [
      "onCommand:extension.createExpressTemplate"
  ],
  "main": "./src/extension.js",
  "contributes": {
      "commands": [
          {
              "command": "extension.createExpressTemplate",
              "title": "Criar Template Express"
          }
      ]
  },
  "scripts": {
      "vscode:prepublish": "npm run compile",
      "compile": "tsc -p ./"
  },
  "devDependencies": {
      "typescript": "^4.0.0",
      "vscode": "^1.1.37",
      "@types/node": "^14.0.0",
      "@types/vscode": "^1.56.0"
  }
}
